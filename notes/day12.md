# Day12 学习笔记：I2C + OLED 显示

## 今天做了什么
今天重点练习 I2C1 和 OLED 显示屏的联调，先不把 DHT11 当主角，而是先把 OLED 显示链路单独打通。

## 今天学到的知识点
1. I2C 只有两根线：SCL 和 SDA
2. I2C 常用开漏输出方式
3. OLED 显示需要先初始化屏幕，再发送显示数据
4. 固定字符串显示只是第一步，动态数字刷新更重要
5. 调试多模块联调问题时，要先把 OLED 和 DHT11 分开测试

## 今日实验内容
- 初始化 I2C1
- 初始化 OLED
- OLED 显示固定字符
- OLED 动态显示计数值

## 今日收获
- 理解了 OLED 本质上是通过 I2C 发送命令和数据来显示内容
- 开始掌握动态刷新显示的方法
- 知道了为什么联调时要先单独验证显示模块

## 遇到的问题
- OLED 设备检测可能卡死
- I2C 地址不对会导致初始化失败
- 屏幕反复清屏会出现闪烁现象

## 今日总结
今天完成了 OLED 单独显示链路的打通，这一步很关键，因为后续温湿度显示、电压显示、菜单显示都会建立在这个基础上。
